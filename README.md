# test

This repository is configured with an AI governance GitHub Actions gate.

The workflow `.github/workflows/ai-governance-verifier.yml` verifies committed
AI work certificates and artifact hashes before merge.
